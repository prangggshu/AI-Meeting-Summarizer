# AI Meeting Notes Summarizer - Production Environment Configuration
# This file contains production-specific environment variables for Vercel deployment
# 
# SETUP INSTRUCTIONS FOR VERCEL DEPLOYMENT:
# 1. Go to your Vercel dashboard
# 2. Select your project
# 3. Go to Settings > Environment Variables
# 4. Add each variable below with your production values
# 5. Set the environment to "Production" for each variable

# =============================================================================
# VERCEL DEPLOYMENT CONFIGURATION
# =============================================================================

# Environment mode (REQUIRED)
NODE_ENV=production

# Base URL for your deployed application (REQUIRED)
# Replace with your actual Vercel domain
BASE_URL=https://your-app-name.vercel.app

# =============================================================================
# AI SERVICE CONFIGURATION (REQUIRED)
# =============================================================================

# Primary AI Service: Groq API (REQUIRED)
# Get your API key from: https://console.groq.com/keys
GROQ_API_KEY=your_production_groq_api_key_here

# Fallback AI Service: OpenAI API (OPTIONAL but recommended)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_production_openai_api_key_here

# AI Service Configuration (Production optimized)
AI_SERVICE_TIMEOUT=25000
AI_MAX_RETRIES=2
AI_RETRY_DELAY=1500
GROQ_MODEL=llama3-8b-8192
GROQ_MAX_TOKENS=4000
GROQ_TEMPERATURE=0.3
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_MAX_TOKENS=4000
OPENAI_TEMPERATURE=0.3

# =============================================================================
# EMAIL SERVICE CONFIGURATION (REQUIRED)
# =============================================================================

# Production SMTP Configuration
# For production, use a reliable email service like SendGrid, Mailgun, or Gmail

# Gmail SMTP (Recommended for small scale)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_production_email@gmail.com
SMTP_PASS=your_gmail_app_password_here

# SendGrid SMTP (Recommended for production scale)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key_here

# Email settings
EMAIL_FROM=your_production_email@gmail.com
EMAIL_FROM_NAME=AI Meeting Summarizer
EMAIL_TIMEOUT=8000
EMAIL_MAX_RECIPIENTS=10
EMAIL_RATE_LIMIT=10

# =============================================================================
# SECURITY CONFIGURATION (REQUIRED)
# =============================================================================

# CORS Configuration (Update with your actual domain)
FRONTEND_URL=https://your-app-name.vercel.app
ALLOWED_ORIGINS=https://your-app-name.vercel.app

# Rate limiting (Production settings)
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=50

# Session security (REQUIRED - Generate a strong random string)
SESSION_SECRET=your_super_secure_random_session_secret_change_this_in_production
BCRYPT_ROUNDS=12

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================

# File upload limits (Production optimized)
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=.txt,.md,.rtf
MAX_CONTENT_LENGTH=500000
MIN_CONTENT_LENGTH=10

# =============================================================================
# PRODUCTION OPTIMIZATION
# =============================================================================

# Production features
ENABLE_COMPRESSION=true
ENABLE_HELMET=true
TRUST_PROXY=true

# Logging (Production settings)
LOG_LEVEL=warn
LOG_API_REQUESTS=false
LOG_AI_REQUESTS=true
LOG_EMAIL_EVENTS=true

# Health check
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_PATH=/health

# =============================================================================
# VERCEL-SPECIFIC CONFIGURATION
# =============================================================================

# Vercel automatically sets these, but you can override if needed:
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=your-app-name.vercel.app
# VERCEL_REGION=iad1

# =============================================================================
# DEPLOYMENT CHECKLIST
# =============================================================================

# Before deploying to production:
# ✅ 1. Set all REQUIRED environment variables in Vercel dashboard
# ✅ 2. Test AI service API keys in staging environment
# ✅ 3. Verify email service configuration with test emails
# ✅ 4. Update BASE_URL and ALLOWED_ORIGINS with actual domain
# ✅ 5. Generate strong SESSION_SECRET (use: openssl rand -base64 32)
# ✅ 6. Configure custom domain in Vercel (optional)
# ✅ 7. Set up monitoring and error tracking
# ✅ 8. Test complete workflow in production environment

# =============================================================================
# MONITORING AND ALERTS (OPTIONAL)
# =============================================================================

# Add these for production monitoring:
# SENTRY_DSN=your_sentry_dsn_for_error_tracking
# DATADOG_API_KEY=your_datadog_key_for_monitoring
# WEBHOOK_URL=your_slack_webhook_for_alerts